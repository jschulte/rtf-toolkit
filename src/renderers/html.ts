/**
 * RTF to HTML Renderer
 */

import type { RTFDocument, HTMLOptions } from '../parser/ast.js';

/**
 * Convert RTF document to HTML
 */
export function toHTML(doc: RTFDocument, options?: HTMLOptions): string {
  // TODO: Implement HTML renderer - Week 1-2
  throw new Error('HTML renderer not yet implemented');
}
